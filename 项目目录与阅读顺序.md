# SO-101 Sim2Real 项目目录与推荐阅读顺序

本文与 `SO101_Sim2Real_一个月学习计划.md` 放在同一工作区目录。实际 Git 仓库位于其子目录 `sim2real/`。

## 项目目标与学习计划

项目把 SO-101 / SO-ARM100 机械臂的 MuJoCo 仿真、Gymnasium 环境、Stable-Baselines3 训练和 Feetech STS3215 硬件部署连接起来。策略使用位置目标动作，并通过动力学与命令路径随机化提高 Sim2Real 迁移能力。当前任务包括 Reach（移动 TCP 到目标）和 Pick-and-Place（抓起杯子并放到目标垫）。ONNX 导出时会把 observation normalization 合入策略图。

一个月计划为 4 周、每周 6 天、每天约 2～4 小时，总投入约 60～90 小时：

- 第 1 周：搭建 WSL/Python/MuJoCo 环境，跑通 smoke test 和 PPO Reach，理解观察、动作、奖励、运动学和 MJCF 场景。
- 第 2 周：理解 PPO 训练入口，建立至少 3 个随机种子的基线，做单变量奖励或超参数实验并分析动作控制。
- 第 3 周：比较无随机化、轻度和组合 Domain Randomization，测试质量、摩擦、阻尼、噪声、延迟及未见参数下的鲁棒性。
- 第 4 周：对照 PPO 与 SAC，尝试 Pick-and-Place，导出 ONNX 并验证仿真部署，整理失败案例、实验图表和智能流体迁移方案。

主要验收成果：可复现环境、PPO Reach 多种子基线、observation/action/reward 说明、一次奖励或动作改动、Domain Randomization 对照、SAC Pick-and-Place 尝试、ONNX 部署验证和最终 README/展示材料。

## 工作区结构

```text
SO101 Sim2Real/                         # VS Code 当前工作区
├── SO101_Sim2Real_一个月学习计划.md   # 四周学习安排
├── 项目目录与阅读顺序.md               # 本文件
├── Quickstart.pdf                      # 快速上手材料
└── sim2real/                            # Git 项目仓库
    ├── assets/                          # MuJoCo 模型与场景资源
    ├── configs/                          # 训练配置
    ├── docs/                             # 架构和部署说明
    ├── outputs/                          # 训练运行结果
    ├── post/                             # 教程/项目文章及配图
    ├── scripts/                          # 辅助脚本
    ├── sim2real/                         # Python 源码包
    ├── tests/                            # 自动化测试
    └── README.md / Makefile / pyproject.toml / requirements.txt
```

### 模型与场景：`sim2real/assets/so101/`

- `assets/`：MJCF 引用的配套几何、网格或材质等资源。
- `joints_properties.xml`：关节属性配置。
- `so101_new_calib.xml`：SO-101 校准版机器人模型；README 提醒该上游模型应保持原样，需要更新时通过 `scripts/fetch_assets.sh` 同步。
- `reach_scene.xml`：Reach 场景，包含机器人、地面和 mocap 目标。
- `pickplace_scene.xml`：抓取放置场景，包含机器人、杯子和红/绿目标垫。
- `scene.xml`：通用 MuJoCo 场景。

### 训练配置：`sim2real/configs/`

- `reach.yaml`：默认 PPO Reach 配置。
- `reach_hard_dr.yaml`：更困难的 Reach 与 Domain Randomization 配置。
- `sac_reach.yaml`：SAC Reach 配置。
- `pickplace.yaml`：Pick-and-Place 基础配置。
- `pickplace_sac.yaml`、`pickplace_sac2.yaml`～`pickplace_sac5.yaml`：SAC 抓取放置的不同实验配方；README 将 `pickplace_sac5.yaml` 标为推荐配方。
- `pickplace_stage1.yaml`、`pickplace_stage2.yaml`：分阶段抓取放置训练配置。

每个 YAML 把一次运行的任务、算法、超参数及环境参数集中起来；比较实验时应确认只改变计划中的变量。

### Python 核心包：`sim2real/sim2real/`

- `config.py`：读取 YAML 并构造带类型的运行配置。
- `env_factory.py`：创建任务环境、向量化环境以及 normalization 等训练/评估管线。
- `train.py`：Stable-Baselines3 PPO/SAC 训练入口。
- `eval.py`：加载训练策略并计算成功率、距离等评估指标。
- `visualize.py`：打开交互式 MuJoCo viewer 或渲染 rollout 视频。
- `export_policy.py`：将策略导出为 ONNX，并将 normalization 合入模型。
- `__init__.py`：Python 包初始化文件。

`envs/` 环境实现：

- `base.py`：`SO101MujocoBase` 共用逻辑，包括控制、随机化和碰撞处理。
- `so101_reach.py`：只用本体感觉状态的 Reach 任务，负责任务观察、动作、奖励和结束条件。
- `so101_pickplace.py`：包含夹爪和可抓取杯子的 Pick-and-Place 任务。
- `__init__.py`：环境子包初始化。

其他子目录：

- `utils/kinematics.py`：正向运动学；仿真与真机共用运动学，以保持 TCP 计算一致。
- `wrappers/command_path.py`：命令路径包装器，用于动作延迟、动作噪声等 command-path 随机化。
- `deploy/deploy_so101.py`：在 MuJoCo stand-in 或 SO-101 上运行策略的部署控制循环。
- `deploy/feetech_bus.py`：Feetech 舵机总线通信。
- `__pycache__/` 与 `.pyc`：Python 自动生成的导入缓存，不是业务源码。

### 其余目录与文件

- `docs/HOW_IT_WORKS.md`：从场景、观察、动作、奖励、Domain Randomization、训练到部署的逐步架构学习指南。
- `docs/DEPLOYMENT.md`：策略部署说明和硬件接口相关注意事项。
- `outputs/ppo_reach_20260911_120222/`、`outputs/ppo_reach_20260911_160410/`：已有 PPO Reach 运行记录目录。
- `outputs/smoke/`：短时 smoke test 的运行产物。训练目录通常用于保存模型、日志和指标；具体子文件随运行配置而异。
- `post/how-to-train.md`：训练操作教程。
- `post/README.md`：`post/` 文章和材料说明。
- `post/assets/`：文章或演示配图，例如 Pick-and-Place 场景图片。
- `scripts/fetch_assets.sh`：获取或同步机器人模型资源。
- `scripts/setup.sh`：环境安装辅助脚本；标准流程由 Makefile 的 setup 目标调用。
- `scripts/hybrid_pickplace.py`：完整任务 runner，使用脚本式拾取与 RL 策略组合。
- `scripts/watch_skills.py`：训练 curriculum skills 的实时 viewer 辅助脚本。
- `tests/`：快速 CPU 自动化测试，覆盖环境、随机化、抓取、奖励和正向运动学；README 说明共有 28 个测试。
- `.gitignore`：排除不应提交的缓存、模型或运行产物。
- `Makefile`：常用命令入口，包括 setup、smoke、train、TensorBoard 和策略查看等。
- `pyproject.toml`：Python 包构建与安装配置。
- `requirements.txt`：Python 依赖清单。
- `README.md`：项目目标、数据流、Quickstart 和仓库目录总览。
- `so101_sim2real.egg-info/`：安装时生成的 Python 包元数据。

## 推荐阅读顺序

1. 工作区根目录的本计划和 `sim2real/README.md`：先了解目标、任务和训练/部署总流程。
2. `sim2real/docs/HOW_IT_WORKS.md`：按设计顺序理解场景、观察、动作、奖励和随机化。
3. `sim2real/configs/reach.yaml`：查看默认 Reach 的任务与算法参数。
4. `sim2real/sim2real/assets/so101/` 中的 `reach_scene.xml` 与 `so101_new_calib.xml`：对应理解仿真场景、关节和机器人模型。
5. `sim2real/sim2real/envs/base.py`、`so101_reach.py`、`so101_pickplace.py`：读共用环境接口，再读两个任务如何定义观察、动作和奖励。
6. `sim2real/sim2real/utils/kinematics.py` 与 `wrappers/command_path.py`：理解 TCP 计算、延迟和动作噪声。
7. `sim2real/sim2real/config.py`、`env_factory.py`、`train.py`：沿配置进入环境，再进入 PPO/SAC 采样和更新流程。
8. `eval.py`、`visualize.py`、`outputs/`：评估成功率和距离，观察策略 rollout 并记录结果。
9. `export_policy.py`、`docs/DEPLOYMENT.md`、`deploy/`：完成 ONNX 和 Feetech 部署链路。
10. 最后再读 `configs/reach_hard_dr.yaml`、`pickplace_sac5.yaml`、`tests/` 和 `post/how-to-train.md`，开始对照实验并沉淀结论。

README 中的典型闭环是：`make setup` → `conda activate sim2real` → `make smoke` → `make train`；训练完成后用 `eval.py` 和 `visualize.py` 查看结果，再由 `export_policy.py` 导出 ONNX。